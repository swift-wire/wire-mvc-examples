// Wire-aware opt-in marker. Its presence tells a consuming target's build plugin to re-parse this module's
// sources — which is how both `@YAMLBody`'s obligation and `@YAMLResponse`'s mode reach the generator.
// Presence-only.
